@model Cart
@{
    int numOfCart = 0;
    if(Model != null)
    {
        numOfCart = Model.Items.Count;
    }
}
<span class="badge text-secondary border border-secondary rounded-circle"
      style="padding-bottom: 2px;">@numOfCart</span>
